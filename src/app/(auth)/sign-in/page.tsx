import SignInForm from "@/components/form/SignInForm";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="w-full">
      <SignInForm />
    </div>
  );
};

export default page;
